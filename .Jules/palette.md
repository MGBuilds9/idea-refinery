## 2026-01-18 - Login Accessibility **Learning:** The "Restricted System" aesthetic often leads to form inputs without visual labels, which is a major accessibility barrier. Relying on placeholders is insufficient as they disappear on typing and aren't always read by screen readers. **Action:** Always add `aria-label` to inputs when visual labels are omitted for stylistic reasons.
